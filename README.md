# 🛍️ Customer Segmentation Explorer

![Python](https://img.shields.io/badge/python-3.12-blue)
![scikit--learn](https://img.shields.io/badge/scikit--learn-1.6.1-orange)
![Streamlit](https://img.shields.io/badge/streamlit-app-ff4b4b)
![License](https://img.shields.io/badge/license-MIT-green)

Groups customers into three behavioral segments — **Daily**, **Promotion**, and **Weekend** — using K-Means clustering on shopping behavior data, with an interactive Streamlit app for scoring new customers on the fly.

## Table of Contents
- [Overview](#overview)
- [Dataset](#dataset)
- [Approach](#approach)
- [Cluster Profiles](#cluster-profiles)
- [Project Structure](#project-structure)
- [Setup](#setup)
- [Usage](#usage)
- [Known Issues](#known-issues)
- [Limitations & Next Steps](#limitations--next-steps)
- [License](#license)

## Overview

Marketing teams often want to target promotions more precisely than a one-size-fits-all campaign allows. This project uses unsupervised K-Means clustering on four behavioral attributes to group customers into three segments, then wraps the trained model in a Streamlit app that assigns a segment to any new customer profile in real time.

## Dataset

**Source:** synthetic data generated with `numpy.random.seed(42)` — 100 customers, built for demonstration purposes rather than sourced from real transactions.

| Feature | Range | Description |
|---|---|---|
| `Age` | 18–64 | Customer age |
| `Average_Spend` | $5–50 | Average spend per visit |
| `Visits_per_Week` | 1–7 | Shopping frequency |
| `Promotion_Interest` | 1–10 | Self-reported interest in promotions |

No missing values; all features are numeric, so no categorical encoding is needed.

## Approach

1. **Elbow method** — SSE plotted across `k = 1` to `10` to sanity-check that 3 clusters is a reasonable choice.
2. **Clustering** — `KMeans(n_clusters=3, random_state=42)` fit directly on the four raw features. *(No feature scaling was applied — see [Known Issues](#known-issues).)*
3. **Labeling** — clusters mapped to descriptive segment names based on their profile.
4. **Evaluation** — silhouette score computed to check cluster separation quality.

## Cluster Profiles

Computed from the training data (100 customers):

| Segment | Size | Avg Age | Avg Spend | Avg Visits/Week | Avg Promo Interest |
|---|---|---|---|---|---|
| Daily | 31 | 55.0 | $36.62 | 4.23 | 5.48 |
| Promotion | 35 | 26.6 | $29.43 | 4.14 | 6.71 |
| Weekend | 34 | 42.7 | $14.72 | 4.46 | 5.29 |

**Silhouette Score: 0.37** — moderate, reasonable cluster separation (0 = overlapping clusters, 1 = well-separated).

> **Honest note:** `Visits_per_Week` is fairly similar across all three segments (4.1–4.5) in this dataset. `Age` and `Average_Spend` are what actually drive the cluster split, with `Promotion_Interest` as a secondary factor. The segment names (Daily/Promotion/Weekend) are descriptive labels chosen when building the notebook, not literal visit-frequency categories — see [Known Issues](#known-issues) for why.

## Project Structure

```
customer-segmentation-explorer/
├── README.md
├── requirements.txt
├── Customer_Segmentation.ipynb   # data generation, clustering, evaluation, export
├── kmeans.pkl                    # trained KMeans model (generated by the notebook)
├── app.py                        # Streamlit app
└── screenshots/
    └── app_demo.png
```

## Setup

```bash
git clone <your-repo-url>
cd customer-segmentation-explorer
python3.12 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

## Usage

### Run the notebook

```bash
jupyter notebook Customer_Segmentation.ipynb
```

Running all cells regenerates the synthetic dataset (fixed seed, so results are reproducible), fits K-Means, evaluates it, and re-saves `kmeans.pkl`.

### Run the Streamlit app

```bash
streamlit run app.py
```

Enter a customer's age, average spend, visit frequency, and promotion interest, then click **Assign Segment** to see their predicted group, distance to each cluster center, and where they fall relative to the training data.

![App demo placeholder](screenshots/app_demo.png)

## Known Issues

- The source notebook defines **two conflicting cluster-to-label mappings**: the `cluster_names` dict (`{0: "Daily", 1: "Promotion", 2: "Weekend"}`), which is what actually produced the `Customer Group` column in the data, and the `clustering()` helper function's if/elif logic, which swaps clusters 1 and 2. The Streamlit app uses the dict mapping, since that's the one that generated the labeled data. If you re-run or extend the notebook, fix the helper function to match.
- **No feature scaling** was applied before clustering, so `Age` and `Average_Spend` (larger numeric ranges) dominate the Euclidean distance calculation over `Visits_per_Week` and `Promotion_Interest`. This is consistent with the profile table above, where spend and age vary widely between segments but visit frequency barely does.

## Limitations & Next Steps

- Apply `StandardScaler` to all four features before clustering, then re-evaluate the silhouette score and re-check whether the resulting cluster profiles are more balanced across all four dimensions.
- `k=3` was chosen by visually inspecting the elbow curve — compare silhouette scores across `k = 2` to `6` to confirm 3 is actually optimal, not just plausible.
- The dataset is synthetic; validate this pipeline against real customer transaction data before using it for actual marketing decisions.
- Consider renaming segments based on the profile table (e.g. "Big Spenders," "Promo-Driven," "Budget Regulars") so the names reflect what the data shows rather than an assumed visiting cadence.

## License

MIT — see [LICENSE](LICENSE).
